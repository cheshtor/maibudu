<view class="container">
    <view class="share-code">
        靡不有初，鲜克有终。
    </view>

    <scroll-view scroll-y class="share-list">
        <m-share-card wx:for="{{shelves}}" wx:key="exporterId" text="{{item.nickname}}的书架" avatar="{{item.avatar}}" code="{{item.code}}"></m-share-card>
    </scroll-view>
</view>