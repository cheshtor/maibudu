<view class="outer" bindtap="showBookDetail">
    <van-card
        currency=""
        title="{{computedTitle}}"
        thumb="{{cover}}">
        <text slot="desc">{{computedAuthor}}</text>
        <text slot="price">{{computedPublisher}}</text>
    </van-card>
</view>