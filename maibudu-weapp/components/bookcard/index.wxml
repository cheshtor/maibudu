<view class="outer">
    <van-card
        currency=""
        title="{{title}}"
        thumb="{{cover}}">
        <text slot="desc">{{author}}</text>
        <text slot="price">{{publisher}}</text>
    </van-card>
</view>